# Florin 手机端界面设计规范（Mobile-First）

> 版本 v1 · 2026-09-05 · 基准屏 390×844（iPhone 14）
> 适用范围：cnflorin.com 手机端。**桌面端已定稿，本规范不动桌面样式。**

---

## 一、布局：移动优先

### 1.1 画布与栅格

| 项 | 值 | 说明 |
|---|---|---|
| 基准宽度 | 390px | 适配区间 360–430px |
| 栅格 | 4 列基线 | 列宽 = (屏宽 − 32 − 3×间距) / 4 |
| 容器留白 | 左右各 16px | 比桌面 20px 收窄，换取内容宽度 |
| 间距阶梯 | 8 / 12 / 16 / 22 / 30px | 全部 4 的倍数 |
| 卡片圆角 | 14px（大）/ 10px（小） | 与桌面一致 |
| 区块上下留白 | 22px | 桌面 56–72px，手机减半 |

### 1.2 断点策略（只保留两档）

```
默认样式        → 手机（≤767px）
min-width:768px → 平板 / 桌面（沿用现有桌面样式，不改）
```

现有代码里 `max-width:560/640/760/900/1024` 等零散降级规则，逐块收敛到这两档。

### 1.3 各区块列数

| 区块 | 手机 | 桌面（不动） |
|---|---|---|
| 产品列表 `.product-grid` | **1 列全宽** | 4 列 |
| 首页产品行 `.product-grid--5` | **1 列全宽** | 5 列 |
| 按甲型选购 | **3 列** | 4 列 |
| 品类条 | **横向滑动**（92px 卡） | 平铺 |
| 三大区域 | **1 列** | 3 列 |
| 评价 / 博客 | **1 列** | 4 列 |
| 深色页脚 | **1 列** | 5 列 |
| 信任条 confidence | **2×2** | 4 列 |

> 为什么产品卡必须 1 列：B2B 卡承载「图 + 分类 + 名称 + 价格 + MOQ/样品/OEM/新品 等 4–6 个标签 + CTA」。
> 实测 2 列时每卡仅 167px，标签浮在图上叠 3–5 行把产品图糊掉，卡片高度 356/387/390px 参差 —— 这是当前「乱」的根因。

---

## 二、配色方案

### 2.1 主色板（沿用品牌）

| 角色 | 色值 | 用途 |
|---|---|---|
| 页面背景 | `#FAF8F5` | 米白 |
| 卡片 / 面 | `#FFFFFF` | 白 |
| 浅金底 | `#F3EBD8` | 区块交替背景 |
| 主文字 | `#222222` | 深炭灰 |
| 辅助文字 | `#5A5550` | 说明文案 |
| 弱化文字 | `#8C857D` | 标签、元信息 |
| 分割线 | `#E8E2DA` / `#D9D0C4` | 边框 |
| 品牌金 | `#D4AF37` | 点缀、描边、图标 |
| 深金 | `#B8941F` | 金元素强调 |
| 行动红 | `#C0392B` | Hero CTA、链接 |
| 深色区 | `#121212` | 订阅 + 页脚 |

### 2.2 ⚠ 必须修正：金色按钮对比度

| 组合 | 对比度 | 结论 |
|---|---|---|
| 白字 on 金 `#D4AF37` | **2.1 : 1** | ❌ 远低于 WCAG AA 4.5:1，手机户外不可读 |
| **深炭灰 `#222` on 金 `#D4AF37`** | **7.6 : 1** | ✅ 达标 AA/AAA |
| 白字 on 深炭灰 `#222` | **15.9 : 1** | ✅ 次选方案 |

**结论：手机端主 CTA 改为「金底 + 深炭灰字」**，视觉仍是品牌金，只换文字色，改动极小。
（桌面端已定稿不动；若后续认可，可同步到桌面。）

### 2.3 使用比例

- 金色 = **点缀色**，占界面 ≤10%（图标、描边、标签、强调线）
- 米白/白 = 背景 80%
- 深炭灰 = 文字与主要按钮
- 红色 `#C0392B` 仅用于 Hero 主 CTA 与「查看全部」链接

---

## 三、字体方案

### 3.1 字体家族

| 类型 | 字体 | 字重 |
|---|---|---|
| 标题 / 数字 / 标签 | **Jost** | 500 / 600 / 700 |
| 正文 / 表单 | **Inter** | 400 / 500 / 600 |
| 中文回退 | PingFang SC / Microsoft YaHei | — |

自托管 woff2（现有 `/assets/fonts/`），不依赖 Google Fonts。

### 3.2 移动端字号阶梯

| 层级 | 字号 | 行高 | 字重 | 示例 |
|---|---|---|---|---|
| Hero 标题 | 26px | 1.18 | Jost 600 | 假睫毛 · 穿戴甲 · 美瞳 |
| 页面 H1 | 24px | 1.25 | Jost 600 | 全部产品 |
| 区块标题 H3 | 19–22px | 1.25 | Jost 600 | 新品上架 |
| 卡片标题 | 15.5px | 1.35 | Inter 600 | 黑金水钻奢华甲 |
| **正文** | **16px** | **1.65** | Inter 400 | 产品描述 |
| 辅助文字 | 13px | 1.6 | Inter 400 | 说明、元信息 |
| 标签 | 11.5px | 1.35 | Inter 600 | MOQ / OEM |
| 小标签（eyebrow） | 10.5px | — | Jost 700 + 字距 2px | NEW ARRIVALS |

> **正文 16px 是硬性要求**：iOS Safari 对 <16px 的输入框与正文会在聚焦时自动放大页面，造成布局跳动。

---

## 四、组件规范（移动端）

### 4.1 产品卡（核心）

```
┌────────────────────────────┐
│  产品图 1:1 全宽（358px）    │  ← aspect-ratio:1/1，锁定等高
├────────────────────────────┤
│ [特价][MOQ 10][样品][OEM]   │  ← 标签平铺在图下方，可换行
│ [新品][热销]                │     桌面端才浮回图片左上角
│ 穿戴甲                      │  ← 分类，10.5px 大写
│ 黑金水钻奢华甲（N01）        │  ← 最多 2 行截断
│ From $2.80 / 套             │  ← Jost 700 / 17px
│ ┌────────────────────────┐ │
│ │      查 看 详 情        │ │  ← 金底深字，高 48px，全宽
│ └────────────────────────┘ │
└────────────────────────────┘
```

- 图片锁定 `aspect-ratio:1/1` → 卡片等高，网格不再错落
- 名称 `-webkit-line-clamp:2` + `min-height:2.6em`
- CTA `min-height:48px`，全宽

### 4.2 触控与安全区

| 元素 | 最小尺寸 |
|---|---|
| 图标按钮 / 汉堡 | 40×40（建议 44） |
| 主 CTA | 高 48px |
| 输入框 / 下拉 | 高 48px |
| 分页按钮 | 42×42 |
| 底部悬浮（WhatsApp） | 54×54，距底 20px |
| 列表底部留白 | 70px（避让悬浮按钮） |

### 4.3 头部与导航

- 顶栏 36px（最低起订提示，可关闭）
- 头部 58px，`sticky` 吸顶 + 毛玻璃
- 桌面导航 `≥1024px` 才显示；手机用 **汉堡 + 右侧抽屉**（现有实现可用）
- 语言切换胶囊放头部，搜索与购物车 38px 圆形按钮

---

## 五、落地清单（对应代码改动）

| # | 改动 | 文件 | 状态 |
|---|---|---|---|
| 1 | `body{overflow-x:clip}` 防横向滚动兜底 | globals.css | ✅ 已完成 |
| 2 | 断点规范注释 + 统一到 768/1024 两档 | globals.css | ✅ 已完成 |
| 3 | 产品列表手机端 1 列 | globals.css | ✅ 已完成 |
| 4 | 标签移出图片 → 平铺在信息区（桌面浮回） | ProductCard.tsx + globals.css | ✅ 已完成 |
| 5 | 卡片名称 2 行截断、CTA 48px、图片锁 1:1 | globals.css | ✅ 已完成 |
| 6 | 甲型区手机 3 列 | globals.css | ✅ 已完成 |
| 7 | 深色页脚手机 1 列 | globals.css | ✅ 已完成 |
| 8 | **CTA 改金底深炭灰字（7.6:1）** | globals.css | ⬜ 待确认 |
| 9 | 正文 16px、输入框 48px/16px | globals.css | ⬜ 待确认 |
| 10 | 筛选器手机端双列全宽 | CollectionPage | ⬜ 待确认 |

---

## 六、验收标准

1. 390 / 768 / 1280 三档视口截图对比，无布局错乱
2. `document.scrollWidth === clientWidth`（零横向滚动）
3. 产品卡：图片无遮挡、卡片等高、CTA 触控区 ≥48px
4. 正文与输入框 ≥16px（iOS 不自动放大）
5. 桌面端（≥768px）视觉**零回归**
