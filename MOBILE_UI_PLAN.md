# florin-b2b 移动端 UI 整改计划

> 生成时间：2026-09-05 · 基于线上 cnflorin.com 390px（iPhone 14 视口）实测诊断
> 原则：**不重做、不换技术栈、不碰数据层**，只动 `globals.css` + 少量组件结构

---

## 一、实测诊断结论

用真实浏览器以 iPhone 14（390×844）视口访问线上站，扫描全部元素的渲染数据：

| 检查项 | 结果 | 结论 |
|---|---|---|
| viewport meta | `width=device-width, initial-scale=1` ✅ | 正常 |
| 横向滚动（首页/PDP） | `docScrollW = 390 = vw`，0 处溢出 ✅ | 页面没有"撑爆" |
| 产品详情页 | 单列 350px，无溢出 ✅ | PDP 基本正常 |
| **产品卡片宽度** | **167px/张（2 列）** ❌ | **"乱"的主因** |
| **卡片内标签** | 每卡 4–6 个，浮在图上叠 **3–5 行** ❌ | 糊住产品图 |
| 卡片高度 | 356 / 387 / 390px 参差 ❌ | 网格错落不齐 |
| 页脚 | 挤成 2 列 × 161px ❌ | 拥挤 |

**一句话根因：不是布局崩了，是信息密度塞错了地方。**
B2B 产品卡（价格 + MOQ + Sample + OEM + New + Best 六类信息）被压进 167px 的格子，
标签浮在图片上堆 3–5 行把图糊掉，卡片高度参差 → 整屏观感"乱、奇怪"。

---

## 二、"有没有模板"——直接回答

现在这套是**手写 CSS（766 行）+ 自定义类名**，不是任何模板。可选路线对比：

| 路线 | 要做什么 | 工期 | 风险 | 结论 |
|---|---|---|---|---|
| **A. 修现有 CSS（推荐）** | 补断点体系 + 重构卡片移动端样式 | 1–2 天 | 低，不碰已验收链路 | ✅ |
| B. 引入 Tailwind CSS | 重写 15 个组件全部 className | 3–5 天 | 中高 | ❌ 得不偿失 |
| C. 换 UI 组件库（shadcn 等） | 页面级重写 | 1–2 周 | 高 | ❌ 等于重做 |
| D. 套现成 Next.js 模板 | 前端整体重做 + 重接数据层 | 1–2 周 | 极高 | ❌ 等于重做 |

**推荐 A 的理由：**
1. 老板之前定过规矩"不要重做"——23 产品链路刚验收通过，重做前端会把已验证的东西推倒。
2. 实测证明 PDP、区块单列化都是好的，坏的只有卡片密度和断点体系——**是"补"，不是"换"**。
3. 手写 CSS 改起来零依赖、零构建变更，改完即所见即所得。

---

## 三、分阶段整改（每阶段独立提交、独立验证、可单独回滚）

### 阶段 0 · 断点体系统一（半天，不动视觉）
- 统一 4 档断点：`≤640`（手机）/ `641–900`（大手机/小平板）/ `901–1024`（平板）/ `>1024`（桌面）
- 现有 18 个媒体查询里 mobile-first（min-width）与 desktop-first（max-width）**混用**，逐个归一
- 补全局兜底：`html,body{overflow-x:clip}` + 长词 `overflow-wrap:anywhere`
- 验证：三档视口截图对比，确认视觉零变化

### 阶段 1 · 产品卡片重构（半天，解决 80% 观感）⭐ 最高优先级
- **移动端标签下移**：从"浮在图上"改到图片下方一行，超过 2 个折叠为 `+N`
- 图片锁定 `aspect-ratio:1/1`，消灭 163 vs 197px 的高度差
- 产品名 `line-clamp:2`（已有 min-height，补 clamp）
- 卡片按钮移动端全宽、高度 ≥44px（触控标准）
- 验证：卡片高度一致、图片完整可见、标签不糊图

### 阶段 2 · 首页与其余区块（半天）
- `shapes-grid`（按甲型选购）移动端 3 列小图标样式（甲型图是图标性质，2 列太大、3 列更精致）
- 页脚 `footer-dark__grid` 移动端 1 列（或折叠手风琴），消灭 161px 挤压
- Hero 标题/副标题字号 `clamp()` 化，小屏自动缩放
- 品类横滑条（cat-strip）**保留**——横滑交互在移动端是正确做法

### 阶段 3 · 列表页筛选与表单（半天）
- 筛选器 select 移动端全宽、双列排布
- 规格表包横向滚动容器（`overflow-x:auto`）
- 询盘表单移动端单列（已部分支持，查漏补缺）

---

## 四、验收标准（每阶段统一执行）

1. 390 / 768 / 1280 三档视口截图对比，无布局错乱
2. `docScrollW === clientWidth`（零横向滚动）
3. 产品卡：图片无遮挡、卡片等高、按钮可点区 ≥44px
4. 桌面端（>1024px）视觉**零回归**（对比改前截图）
5. 生产部署后手机实机抽查首页 / 列表页 / 详情页

---

## 五、已拍板（2026-09-05 老板确认）

| # | 决策点 | 结论 |
|---|---|---|
| 1 | 产品卡移动端列数 | ✅ **1 列全宽**（350px，信息完整，标签一行放下）|
| 2 | 工期 | 合计约 2 天，拆 4 个阶段 |
| 3 | 上线方式 | ✅ **分 4 次小步上线**：每阶段单独 commit + 部署 + 三档视口验证，可单步回滚 |

执行顺序：阶段 0 → 1 → 2 → 3，每步上线后手机实机抽查确认再进下一步。
